"vulnerabilities": "1. Initiate an automatic landing sequence so that the vehicle enters Land::on_active().\n2. Simultaneously degrade or block GPS updates (e.g., via jamming or disabling the GPS sensor driver) so that get_global_position() stops receiving new data.\n3. Trigger the landing abort condition (for example, via a high‑level command or navigator logic) causing abort_landing() to return true.\n4. The code will read the stale lat/lon/alt values from get_global_position() and send a VEHICLE_CMD_DO_REPOSITION to those outdated coordinates.\n5. The vehicle attempts to reposition to the wrong location, potentially causing a safety hazard or flight loss.",
"thoughts": "In the Land::on_active() function, when abort_landing() returns true, the code reads _navigator->get_global_position()->lat, lon, and alt without any check on the timestamp or data freshness. If GPS updates stop (e.g., due to signal loss) or are delayed, get_global_position() will continue to return the last cached values. Using those stale coordinates in VEHICLE_CMD_DO_REPOSITION can cause the vehicle to navigate to an out‑of‑date location rather than its true current position. There is no prior validation of how recent the position fix is, so an attacker or a failure scenario that interrupts GPS updates right before or during landing could exploit this to direct the drone to the wrong place upon aborting landing."
,
"testcase": "TEST_CASE(\"LandingAbort_UsesStaleGPSCoordinates\", \"[multicopter]\") \n{\n    AutopilotTester tester;\n    tester.connect(connection_url);\n    tester.wait_until_ready();\n\n    // Arm and take off to hover at 10m above home\n    tester.arm_vehicle();\n    tester.takeoff();\n    tester.wait_until_hovering();  // Drone now at ~10m over home\n\n    // Begin automatic landing sequence\n    tester.set_flight_mode(\"LAND\");\n    // Wait until the drone descends to ~1m above ground\n    tester.wait_until_reach(0.0, 0.0, 1.0, 5.0, 1.0);\n\n    // Simulate GPS update loss by pausing position updates\n    tester.do_pause();  // GPS position remains at last known (home lat/lon, alt~1m)\n\n    // Trigger landing abort by switching to LOITER\n    tester.set_flight_mode(\"LOITER\");\n\n    // The vehicle should read stale GPS (home lat/lon at ~1m) \n    // and issue a reposition (VEHICLE_CMD_DO_REPOSITION) to those outdated coords.\n    // Expect drone to climb back to ~10m over home (stale pos) as a result:\n    tester.wait_until_reach(0.0, 0.0, 10.0, 2.0, 1.0);\n}"
