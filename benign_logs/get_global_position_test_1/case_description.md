"vulnerabilities": "1. Cause the GPS/global position update to stop (e.g., block the ORB publication or simulate an outage).  2. Keep the Navigator main loop running so that have_geofence_position_data may be false and geofence checks are skipped.  3. Publish a new transponder_report on _traffic_sub to trigger Navigator::check_traffic().  4. check_traffic() calls get_global_position() and passes stale lat/lon/alt to detect_traffic_conflict().  5. The algorithm runs on outdated coordinates, potentially missing a real conflict or raising a false positive.  6. Similarly, call run_fake_traffic() and observe fake traffic computed at the stale position.",
"thoughts": "While the RTL time‐estimate path explicitly tests the timestamp on global_position (hrs_elapsed_time() < 10 s), other critical components—namely traffic conflict detection in Navigator::check_traffic() and fake traffic injection in Navigator::run_fake_traffic()—use get_global_position()->lat/lon/alt unconditionally. They do not check that the underlying global_position.timestamp is recent. If GPS updates stall (e.g., vehicle in a canyon or GPS disabled), _global_pos will retain old data and these routines can spuriously declare conflicts or inject fake traffic at the wrong location, or worse miss a real conflict because the stale position is out of range."
,
"testcase": "TEST_CASE(\"StaleGPS_Skips_Geofence_Checks\", \"[multicopter]\")\n{\n    AutopilotTester tester;\n    tester.connect(connection_url);\n    tester.wait_until_ready();\n\n    // Arm and take off to 10 m altitude\n    tester.arm_vehicle();\n    tester.set_flight_mode(\"TAKEOFF\");\n    tester.wait_until_hovering();\n    // At this point the drone is at ~10 m above home with fresh GPS\n\n    // Upload an exclusion geofence from N=45→55 m, E=–10→10 m (doesn't overlap home or RTH)\n    tester.upload_relative_polygonal_geofence(\n        {{45.0, -10.0},\n         {45.0,  10.0},\n         {55.0,  10.0},\n         {55.0, -10.0},\n         {45.0, -10.0}},\n        \"exclusion\"\n    );\n    tester.set_geofence_action(\"WARNING\");\n    // Now geofence is armed around the planned flight corridor\n\n    // Simulate GPS outage by pausing global_position updates (stale lat/lon/alt)\n    tester.do_pause();\n    // Navigator::have_geofence_position_data becomes false and geofence checks are skipped\n\n    // Fly across the exclusion zone on a straight line from home to (100,0)\n    tester.fly_guided_move_local(100.0, 0.0, 10.0);\n    // Wait until the drone reaches the target (under stale GPS, no geofence violation will trigger)\n    tester.wait_until_reach(100.0, 0.0, 10.0, 1.0, 1.0);\n}"
