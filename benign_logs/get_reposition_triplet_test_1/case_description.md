"vulnerabilities": "1) In any mode other than AUTO_LOITER (e.g., POSCTL), send a VEHICLE_CMD_DO_REPOSITION. rep->current is set.  \n2) Do not switch immediately into Loiter.  \n3) Change the vehicle’s navigation state to AUTO_LOITER (manually or via a command).  \n4) Navigator::run detects mode switch to Loiter with no existing loiter setpoint, calls reset_triplets(), clearing the reposition triplet.  \n5) Loiter::on_activation() now sees rep->current.valid==false and does not reposition.",
"thoughts": "Navigator::run() contains logic to reset all triplets (including the reposition triplet) when switching navigation modes, unless switching specifically from Takeoff→Loiter or into Loiter when a valid loiter setpoint already exists in the position setpoint triplet. When a reposition command arrives while in a non‑Loiter mode, rep->current.valid is set. If the mode then changes to AUTO_LOITER, Navigator::run sees _navigation_mode!=new, new_mode_is_loiter=true, but pos_sp_triplet.current.valid==false (no prior loiter setpoint), so it calls reset_triplets() and clears rep before Loiter::on_activation runs. The reposition command is thus dropped silently."
,
"testcase": "TEST_CASE(\"RepositionCommandDroppedOnLoiterSwitch\", \"[multicopter]\")\n{\n    AutopilotTester tester;\n    tester.connect(connection_url);\n    tester.wait_until_ready();\n\n    // Arm and take off\n    tester.arm_vehicle();\n    tester.takeoff();\n    tester.wait_until_hovering(); // Drone is hovering at takeoff point\n\n    // Switch to a non‑Loiter mode (e.g., MISSION)\n    tester.set_flight_mode(\"MISSION\");\n\n    // Send a reposition command in non‑Loiter mode\n    tester.fly_guided_move_local(20.0, 0.0, 5.0); \n    // rep->current.valid is set by Navigator\n\n    // Immediately switch into Loiter without any prior loiter setpoint\n    tester.set_flight_mode(\"LOITER\");\n\n    // Wait for loiter activation\n    tester.wait_until_hovering();\n    // At this point the reposition command should have been cleared by Navigator::run(),\n    // so the drone remains at its original takeoff position rather than moving 20 m north.\n}"
