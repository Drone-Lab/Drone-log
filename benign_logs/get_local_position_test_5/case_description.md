"vulnerabilities": "1. Arm the vehicle and enter a loiter mode for a rotary‐wing vehicle.\n2. Inject or induce a transient velocity spike in the local position estimate (e.g. feed a large vx or vy into _local_pos).\n3. Trigger Loiter::set_loiter_position (e.g. by requesting a loiter via the navigator).\n4. Observe that the computed loiter setpoint is shifted far from the vehicle by the excessive braking distance.",
"thoughts": "Navigator::calculate_breaking_stop computes a braking distance solely from the current horizontal speed (_local_pos.vx, _local_pos.vy) without any upper bound. A transient spike or overestimation in the velocity leads to an inflated braking_distance. Loiter::set_loiter_position for rotary wings calls this function (via setLoiterItemFromCurrentPositionWithBreaking), so the loiter waypoint is projected far away along the (wrongly) large braking distance."
,
"testcase": "TEST_CASE(\"Loiter Braking Distance Spike\", \"[multicopter]\")\n{\n    AutopilotTester tester;\n    tester.connect(connection_url);\n    tester.wait_until_ready();\n\n    // 1. Arm vehicle\n    tester.arm_vehicle();\n\n    // Ascend to 10 m altitude to enable air‐borne LOITER and horizontal movement\n    tester.do_change_altitude(10.0);\n    tester.wait_until_reach(0.0, 0.0, 10.0, 1.0, 0.5);\n    tester.wait_until_hovering();\n\n    // 2. Enter LOITER mode – vehicle holds current position at ~10 m\n    tester.set_flight_mode(\"LOITER\");\n    tester.wait_until_hovering();\n\n    // 3. Induce a transient horizontal velocity spike via a rapid guided move\n    tester.fly_guided_move_local(100.0, 0.0, 0.0);\n    // Wait for most of the horizontal displacement to ensure the spike is recorded\n    tester.wait_until_reach(90.0, 0.0, 10.0, 5.0, 1.0);\n\n    // 4. Request LOITER again – this calls Loiter::set_loiter_position,\n    //    braking_distance is computed from the spiked velocity\n    tester.set_flight_mode(\"LOITER\");\n    tester.wait_until_hovering();\n\n    // Observe that the loiter setpoint is projected far from the vehicle\n}"
