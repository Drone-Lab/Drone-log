"vulnerabilities": "1. Fly a mission and then trigger Return to Launch (RTL) so the navigator enters MISSION_EXECUTION_MODE_REVERSE. 2. Before or immediately after RTL begins, issue a DO_SET_HOME (or other home‑reset) command with new coordinates/altitude. 3. The next call to index_closest_mission_item() will use the updated home, see that it is now the closest ‘waypoint’, and change the mission index to –1, effectively redirecting the vehicle back to the new home.",
"thoughts": "In reverse‑mode (MISSION_EXECUTION_MODE_REVERSE, used during RTL), index_closest_mission_item() will compare the current closest waypoint distance with the distance to home, using the up‑to‑date home coordinates and altitude. If home is closer than any mission waypoint, the routine sets min_dist_index to –1 (i.e. ‘go home’). Later, set_closest_item_as_current() does _current_mission_index = –1, switching the target mid‑flight. If the home position is changed (e.g. via DO_SET_HOME) before or during RTL, this distance comparison will use the new home, potentially causing the vehicle to re‑select home as the landing point unexpectedly."
,
"testcase": "TEST_CASE(\"Mission reverse‑mode home reset vulnerability\", \"[multicopter]\")\n{\n    AutopilotTester tester;\n    tester.connect(connection_url);\n    tester.wait_until_ready();\n\n    // Upload the fixed mission before takeoff (takeoff to 10m, WP1:100,0,10; WP2:100,100,10; WP3:150,150,15; RTL)\n    tester.upload_mission();\n\n    // Arm and take off to hover at ~10m\n    tester.arm_vehicle();\n    tester.set_flight_mode(\"TAKEOFF\");\n    tester.wait_until_hovering();\n\n    // Start mission execution\n    tester.set_flight_mode(\"MISSION\");\n    // Wait until the drone reaches the second waypoint to ensure mission is underway\n    tester.wait_current_waypoint(2);\n\n    // Trigger Return‑to‑Launch (RTL), entering reverse mission mode\n    tester.set_flight_mode(\"RTL\");\n\n    // Immediately reset home position 50m north of original home (same altitude)\n    tester.change_home_position_rel(50.0f, 0.0f, 0.0f);\n\n    // In reverse mode, the updated home should become the closest point and redirect the vehicle\n    // Verify by waiting until the drone reaches the new home at (50, 0) with altitude ~10m\n    tester.wait_until_reach(50.0, 0.0, 10.0, 5.0, 2.0);\n\n    // Disarm after landing at the new home location\n    tester.disarm_vehicle();\n    tester.wait_until_disarmed();\n}"
