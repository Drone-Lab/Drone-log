"vulnerabilities": "1. Issue a VEHICLE_CMD_NAV_TAKEOFF command → navigator writes takeoff_triplet with desired altitude/position and NAV_STATE changes to AUTO_TAKEOFF.  2. Before the vehicle leaves the ground, switch to AUTO_LOITER mode (e.g., via a HOLD/LOITER command) → reset_triplets() is skipped, so takeoff_triplet remains intact.  3. Immediately switch back into AUTO_TAKEOFF mode without sending another NAV_TAKEOFF command → Takeoff::on_active sees rep->current.valid and uses the stale triplet, causing an unexpected or incorrect takeoff setpoint.",
"thoughts": "The code explicitly skips calling reset_triplets() when transitioning from AUTO_TAKEOFF to AUTO_LOITER so as not to lose the takeoff altitude. However, if the vehicle rapidly toggles out of takeoff mode into loiter and back into takeoff without issuing a new NAV_TAKEOFF command, the old takeoff_triplet remains valid and gets re‑used by Takeoff::on_active(). This stale data may contain an outdated or unintended target altitude/position, causing the vehicle to ‘take off’ to the wrong height or location."
,
"testcase": "TEST_CASE(\"Takeoff stale triplet reuse vulnerability\", \"[multicopter]\")\n{\n    AutopilotTester tester;\n    tester.connect(connection_url);\n    tester.wait_until_ready();\n\n    // Arm the vehicle\n    tester.arm_vehicle();\n\n    // 1. Issue a TAKEOFF command → navigator writes takeoff_triplet and enters AUTO_TAKEOFF\n    tester.set_flight_mode(\"TAKEOFF\");\n    tester.wait_until_hovering(); // Vehicle hovers at the initial takeoff altitude\n\n    // 2. Before vehicle leaves ground, switch to LOITER → reset_triplets() is skipped\n    tester.set_flight_mode(\"LOITER\");\n    // Vehicle remains hovering; takeoff_triplet is still valid but unused\n\n    // 3. Immediately switch back to TAKEOFF without new NAV_TAKEOFF command\n    tester.set_flight_mode(\"TAKEOFF\");\n    // Takeoff::on_active re‑uses stale triplet → vehicle may ascend to wrong altitude\n    tester.wait_until_hovering();\n\n    // Clean up: land and disarm\n    tester.set_flight_mode(\"LAND\");\n    tester.wait_until_disarmed();\n}"
